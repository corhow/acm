#include<iostream>
using namespace std;
int main()
{
	int n; cin >> n;
	int A[100][2], sum=0;
	for (int i = 0; i < n; i++)
		for (int j = 0; j < 2; j++)
		{
			cin >> A[i][j];
			if (j == 1)
				if (A[i][j] >= 2)
					if (A[i][j] - A[i][j - 1] >= 2)
						sum++;
		}
	cout << sum << endl;
}
